import 'package:flutter/material.dart';

class SelectQuestionsForContest extends StatelessWidget {
  const SelectQuestionsForContest({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
